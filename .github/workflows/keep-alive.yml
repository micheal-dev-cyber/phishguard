name: Keep Alive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: curl -s -o /dev/null -w "%{http_code}" --connect-timeout 15 https://sabersouihi-phishguard-ai.hf.space
